import React from 'react'import './App.css'import {Provider, connect, createStore} from "./redux";const reducer = (state, action) => {    if (action.type === 'updateUser') {        return {            ...state,            user: {                ...state.user,                ...action.payload            },        }    } else {        return state    }}const initState = {    user: {        name: 'zhangsan',        age: 12    },    group: {        name: '前端'    }}const store = createStore(reducer, initState)function App() {    return (        <Provider store={store}>            <Father/>            <hr/>            <Son/>            <Group/>        </Provider>    )}const mapStateToProps = (state) => {    return {        user: state.user    }}const mapDispatchToProps = (dispatch) => {    return {        updateUserName: (payload) => {            dispatch({type: 'updateUser', payload})        }    }}const Father = connect(mapStateToProps)(({user}) => {    console.log('father', user)    return <div>        {user.name}    </div>})const Son = connect(mapStateToProps, mapDispatchToProps)((props) => {    console.log('Son')    const {updateUserName, user} = props    const handleChange = (e) => {        updateUserName({name: e.target.value})    }    return <input value={user.name} type="text" onChange={handleChange}/>})const Group = connect((state) => {    return {group: state.group}})(({group}) => {    console.log('Group')    return <div>        <p>Group: {group.name}</p>    </div>})export default App